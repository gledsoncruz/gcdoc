<!-- Main Header -->
<header class="main-header">

  <!-- Logo -->
  <a href="/" class="logo">
    <!-- mini logo for sidebar mini 50x50 pixels -->
    <span class="logo-mini"><b>G</b>C</span>
    <!-- logo for regular state and mobile devices -->
    <span class="logo-lg"><b>GC</b>Docs</span>
  </a>

  <!-- Header Navbar -->
  <nav class="navbar navbar-static-top" role="navigation">
    <!-- Sidebar toggle button-->
    <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
      <span class="sr-only">Toggle navigation</span>
    </a>
    <!-- Navbar Right Menu -->
    <div class="navbar-custom-menu">
      <ul class="nav navbar-nav">
      <li><%= link_to 'Ir para o Site', root_path %></li>
        <!-- User Account Menu -->
        <li class="dropdown user user-menu">
          <!-- Menu Toggle Button -->
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <!-- The user image in the navbar-->
            <%= image_tag current_user.avatar.url, class: "user-image" %>
            <!-- hidden-xs hides the username on small devices so only the image appears. -->
            <span class="hidden-xs"><%= current_user.full_name %></span>
          </a>
          <ul class="dropdown-menu">
            <!-- The user image in the menu -->
            <li class="user-header">
              <!--img src="" class="img-circle" alt="User Image" /-->
              <%= image_tag current_user.avatar.url, class: "img-circle" %>
              <p>
                <%= current_user.full_name %>
                <small>Membro desde <%= current_user.created_at.strftime("%B") %> de <%= current_user.created_at.year %></small>
              </p>
            </li>

            <li class="user-footer">
              <div class="pull-left">
                <%= link_to 'Perfil', edit_user_registration_path, class: "btn btn-default btn-flat" %>
              </div>
              <div class="pull-right">
                <%= link_to('Sair', destroy_user_session_path, :method => :delete, class: "btn btn-default btn-flat") %>
              </div>
            </li>
          </ul>
        </li>
        <li>
          <a href="#" data-toggle="control-sidebar"><i class="fa fa-gears"></i></a>
        </li>
      </ul>
    </div>
  </nav>
</header>
